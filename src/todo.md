Use `perror` more often, i.e. when functions that sets errno fails.

Build man pages with sphinx? At least do something about the need to
write redundant documentation.

GPU accelerated dot detection/fitting?

Disabling color output etc when `isatty` returns 0.

Use transpositions instead of strided convolutions for `fim_gmsooth` etc.
